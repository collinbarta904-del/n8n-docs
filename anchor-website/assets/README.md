# assets

`logo.webp` — the circular brand mark shown in the header (and mobile burger area).

**Note:** the committed `logo.webp` is a generated placeholder anchor. Replace it
with your real logo exported as WebP (square, transparent background — it sits on a
white circle and is scaled to 72% inside it).
