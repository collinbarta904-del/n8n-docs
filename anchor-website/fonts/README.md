# fonts

`GeistPixel-Circle.woff2` — **fallback** display face only.

The primary display font (`BubbledotICG-FinePos`) loads from the OnlineWebFonts CDN,
so the page renders correctly without this file. Drop the real
`GeistPixel-Circle.woff2` here to complete the local fallback in the display stack:

    "BubbledotICG-FinePos", "Geist Pixel Circle", monospace
